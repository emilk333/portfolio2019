import { MouseWheel } from './mouse-wheel.directive';

describe('MouseWheel', () => {
  it('should create an instance', () => {
    const directive = new MouseWheel();
    expect(directive).toBeTruthy();
  });
});
